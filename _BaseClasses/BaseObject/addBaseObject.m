function addBaseObject( baseObject)
%ADDBASEOBJECT calls addObject, so there will be an auto-complete typing for adding objects
%   will throw an error if an object with a same name is already in the BaseObject's data-set
BaseObject.addObject(baseObject);
end
